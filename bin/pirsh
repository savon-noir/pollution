#!/usr/bin/env ruby

require '../lib/pollution'

Pollution::Config.instance.load("./config.yaml")
platform = Pollution::Config.instance.get("provider")


connection = Pollution::Provider.new(platform)
connection.list_servers()
